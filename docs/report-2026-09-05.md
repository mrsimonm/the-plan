# Report — everything that happened (Thu evening → Fri 11:00, 2026-09-05)

Written by Michael (orchestrator). Plain language, chronological, honest about what went wrong and what it cost.

---

## 1. Thursday evening — small things

- **Margo's two TeachBench bugs** (quick-capture buttons covering the Log/save button; voice dictation looping) — Creed diagnosed them, you approved in-session, fixed and pushed as `4c40d45`. Margo still needs to retest.
- **Creed's Planner spec** (`docs/planner-block-categories-spec.md`, categories for calendar blocks) — written, zero code. Still waiting on you: two open design questions + who should build it.
- **Closing-time drill** — you pressed it, cancelled it a few minutes later; nothing was lost.
- **DeepSeek** — router (`ccr`) installed; last step (your key in its browser UI) still open. Nothing runs on DeepSeek yet.

## 2. Friday 00:50 → 03:00 — beta-1 release train (you were asleep, full autonomy)

**Audits first (read-only):** Meredith did both an engineering audit and a phone-viewport UX walkthrough (Creed was asleep and unwakeable). Big correction from that: the plant library **was already merged** (~1,074 species live); the only bug was a counter that said "158 plants".

**Then three waves, one commit each, smoke-tested at phone width, all on `main`:**

| Wave | What shipped | Commits |
|---|---|---|
| 1 — visible | real plant count + Browse-all · six tabs fit on a phone · floating buttons tamed · About/What's-new sheet · Czech placeholders | `c1b3937 db77f68 06f81e3 2b46e31 98d22f9` |
| 2 — reliability | global error capture + Copy diagnostics · one version number (`v1.0.58`) + reload toast · silent failures made visible · storage-nearly-full warning | `5fcf65b bf99655 f49e510 71624a7` |
| 3 — boot & safety | Firebase no longer loads on first launch (0 Google requests, proven twice) · backup nudge + last-backup dates · XSS hardening from a full scan · favicon | `138d35d 33a8a4e 69733da faf88e7` |

**Release tooling:** `tools/smoke/smoke.sh` (boots all 17 screens, fails on console errors — 17/17 pass) · `tools/bump.py` (keeps version + service-worker cache in sync) · `firebase.json`/`.firebaserc` staged (`1322079`).

**Tagged `beta-1` on `1322079`.**

**Two findings worth knowing:**
- There is **no desktop app to rebuild** — "Munder Difflin v0.4.6" is the agent-office harness, not Potting Bench. The web/PWA build is your only customer surface.
- The Firestore/Storage **rules deploy still needs your `firebase login`** (CLI is installed, one command after that).

**Deferred on purpose:** photos → IndexedDB, Czech dictionary gap scan, `watchLang` performance, roster traffic-light, data-layer tests.

## 3. Friday 08:00 → 11:00 — TeachBench audit (your request)

**You reported:** "Can do" questionnaire broken; dialogs open off-centre / non-functional on a phone.

**What we found:**
- **Dialogs:** all 13 TeachBench dialogs measure centred, scrollable and closable at 320/360/390 on the current build. The off-centre behaviour was an **older cached build** on your phone — close and reopen the app.
- **Can do:** the questionnaire itself renders/saves/reloads fine. The real bug: **C2-level students were shown A1/A2 statements** (no C2 pool, level list lacked `c2`). **Fixed and pushed** (`cdd3d8e`) — added 6 proper C2 statements (EN + CS). A second, independent audit confirmed ticks and scores survive a reload.
- **Backup → wipe → restore** for TeachBench is **byte-for-byte perfect** across all 9 data lists.
- Photo dialog "missing close button" — not a bug (has one; only opens with a photo).
- Your **5 local commits from 09:45** (TeachBench menu refactor, photo thumbnails, variegation, propagation, foliage, dictionary) were unpushed; Meredith's push carried them, so they're live now.

**What went wrong (this is the part that cost money for nothing):**
- I added three extra auditors with a **2.5M-token cap each**; all three ran out while driving the app in a browser and wrote **nothing**. ~$5 wasted.
- I re-spawned two on Sonnet with bigger caps and an "append as you go" rule. The **code-review one died again with an empty doc** (~$1). The **teacher-data one** finished 2 of 6 cases (the two above) before its cap (~$2).
- Root cause both times: an agent reading a 30,000-line file "just a bit more" until the budget is gone. The code-review lane is cancelled for good; its two concrete checks are now Meredith's (she's already in that code).
- Dwight went silent for ~an hour; now back and told to finalize with what he has.

**Your rule, saved permanently:** never Fable unless you ask; Opus for code edits, Sonnet for read-only audits; browser-driving workers get ≥6M caps and must write findings incrementally.

**Running right now:** Meredith — fixing the real items from the two audit docs, doing the escaping/unhandled-error pass on TeachBench, checking add-student/add-card dialogs with the keyboard up, then version bump + push + tap-by-tap repro steps for you. Dwight — finalizing his audit doc.

## 4. Cost picture (approximate, from the office floor)

- Orchestrator (me, on Fable): ~$91 total across both days — the single biggest line; your switch to Opus takes effect when this session restarts.
- Dwight: ~$52 (smoke gate, Firebase prep, packaging investigation, TeachBench audit).
- Meredith: see floor (both audits + 15 release commits + today's fixes).
- Wasted on reaped auditors: ~$8 total.

## 5. Still needs you

1. **`firebase login`** in a terminal → reply *done* → rules deploy runs (matters for TeachBench streaks).
2. Close & reopen the app on your phone (drop the cached build), then re-test "Can do" with a C2 student.
3. Beta offering: plants only, or plants + TeachBench?
4. Pick first customers · one real pilot lesson · real-phone keyboard check.
5. Not urgent: Planner spec decisions · DeepSeek key · Margo's retest.
