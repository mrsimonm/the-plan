# TeachBench + Modal/Dialog System — Adversarial Code Review

Read-only review of `index.html`. Grep-driven, narrow-range reads. No code changes made.

Severity legend: **BLOCKER** (breaks core flow / data loss / crash), **SHOULD-FIX** (real bug, workaround exists or impact limited), **NICE** (polish / robustness / a11y).

## Can-do questionnaire

## Modal / dialog system

## TeachBench logic (async, listeners, state, data, i18n, a11y)

## Summary

